FROM --platform=linux/amd64 n8nio/n8n:latest

# Shared Dockerfile for local development and Cloud Run
# SQLite mode, no Redis required
ENV DB_TYPE=sqlite \
    EXECUTIONS_MODE=regular \
    N8N_PORT=5678
